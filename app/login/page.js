'use client';
import React, { useContext } from 'react'
import LoginForm from '../../components/LoginForm'

const page = () => {
  return (
    <div>
      <LoginForm />
    </div>
  )
}

export default page
